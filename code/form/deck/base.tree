
load ../text
  take form text

load ../term
  take form term

form package
  link mark, like text
  link term, list term
  link head, like text
  link host, like text
  link name, like text
  link bear, like text
  link card, list card

form card
  link link, like text
  link package, like package
